{{#let (subscribe @sequence.seq) as |latest|}}
  <textarea
    {{did-insert this.initialValue latest}}
    {{did-update this.updateValue latest}}
    {{on "input" (fn this.update latest)}}
    ...attributes
  ></textarea>
{{/let}}
