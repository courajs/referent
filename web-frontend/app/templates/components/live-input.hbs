{{#let (subscribe @sequence.seq) as |latest|}}
  <input
    {{did-insert (bind this.initialValue) latest}}
    {{did-update (bind this.updateValue) latest}}
    oninput={{action this.update latest}}
    ...attributes
  >
{{/let}}
